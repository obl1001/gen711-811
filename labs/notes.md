notes
Pwd = print working directory 
ls = list files 
clear = clears all the busy stuff
if you only do one carrot thing ' control C cancels the command 
working in shell_data fold 
ls -F gives the directories but with a backslash now 
man is manual commmand that syaing the next comand after man will give you the manual
head allows you to look at whats in your file 
cat prints the whole thing
cd ../ gets you out each time you enter this ../ it will take you a directory out
cd ~ = gets home to your home directory 
cd then enter what working directory you want to work in changes your wokring directory 
hitting tab twice again as your're typing basically acts as ls
command  (rm -fR) is delete this derctroy and remove everything inside of it
cd $HOME also takes you home 
grep command = searches througha file and will only the lines that match what you're searching for 
> is a redirect symbol and after it you put the file of where you want the output to be 